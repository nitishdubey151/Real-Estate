Deployment instructions for this repository (prepared by assistant)

This branch (`deploy/streamlit`) contains the exact files needed to deploy the Streamlit app as your friend ran it.

What I prepared
- `df.pkl` and `pipeline.pkl` — model artifacts generated by executing `Model-Selection.ipynb` in the Codespace.
- `.streamlit/config.toml` — minimal config for headless run.

Deploy to Streamlit Community Cloud (one-click)

1. Go to: https://share.streamlit.io and sign in with your GitHub account (the one that owns this repo: `satyam-sam/Real-Estate`).
2. Click **New app**.
3. Choose repository: `satyam-sam/Real-Estate`.
4. Branch: select `deploy/streamlit`.
5. Main file: enter `Home.py`.
6. Click **Deploy app**.

Notes & safety
- This deployment includes binary pickles (`df.pkl`, `pipeline.pkl`) committed to the repository to reproduce the exact behavior. Only proceed if this data is safe to publish.
- If you prefer private hosting or a secrets-based approach, tell me and I will prepare a deployment flow that downloads these artifacts from a private release or S3 at runtime.

If you want me to add automation (GitHub Actions) for deployment or to push any further changes, reply and I will add them.

Preview in Codespace
- The app is already running inside your Codespace on port `8501`. Use the Codespaces Ports view to open it while testing.

Support
- If deployment fails on Streamlit Cloud, copy the app logs and share them here; I will diagnose and fix the issue without changing source code.
